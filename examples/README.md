# Examples

This folder contains notebooks with examples demonstrating how the toolkit can be leveraged. Note, that running these examples may require downloading packages not required by the base library, e.g. `opencv`, `scikit-image`. The first block of the notebook should indicate when this is the case.